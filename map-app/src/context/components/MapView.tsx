import { useContext } from 'react';
import { PlacesContext } from '../../context';

export const MapView = () => {


    return (
        <div>
            This is where geolocation goes
        </div>
    )
}